name: Auto Tag Release

on:
  push:
    branches:
      - main
    paths:
      - VERSION
      - CHANGELOG.md
      - '.github/workflows/auto-tag-release.yml'

permissions:
  actions: write
  contents: write

concurrency:
  group: auto-tag-release
  cancel-in-progress: false

jobs:
  tag:
    name: Create release tag from VERSION
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Read version
        id: version
        shell: bash
        run: |
          version="$(tr -d '[:space:]' < VERSION)"
          if [[ ! "$version" =~ ^v[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$ ]]; then
            echo "VERSION must contain a semver tag like v1.0.1. Found: $version" >&2
            exit 1
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"

      - name: Create tag if missing
        id: create
        shell: bash
        run: |
          version="${{ steps.version.outputs.version }}"
          git fetch origin "refs/tags/${version}:refs/tags/${version}" || true
          if git rev-parse -q --verify "refs/tags/${version}" >/dev/null; then
            echo "Tag ${version} already exists; no new release tag needed."
            echo "created=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "$version" -m "Release ${version}"
          git push origin "$version"
          echo "created=true" >> "$GITHUB_OUTPUT"

      - name: Dispatch tagged release workflows
        if: ${{ steps.create.outputs.created == 'true' }}
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          version="${{ steps.version.outputs.version }}"
          for workflow in release-macos.yml release-linux.yml release-windows.yml; do
            gh workflow run "$workflow" --ref "$version"
          done
