# SPDX-License-Identifier: AGPL-3.0-or-later

[package]
name = "crossscp-config"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
name = "crossscp_config"
path = "src/lib.rs"

[dependencies]
crossscp-core = { path = "../crossscp-core" }

[lints]
workspace = true
